Fonctionnement du site :

/app : elements graphique et pages
/public : assets et index.php (fichier principal)
/src : code backend
/vendor : librairies composer

Système de connexion:
!! Les formulaires doivent êtres en POST (<form action=POST>...</form>) !!

Le noms des inputs à mettre + type

- Inscription: 
Champs : name(text),surname(text),email(text ou email),password(password),discord(text),signinsend(submit)

- Login: 
Champs : lemail(text),lpassword(password),loginsend(submit)

- Confirmation:
Champs : code(number),confirmsend(submit)

--------------------------------------------
Affichage des erreurs:
- Mettre ce code ou les erreurs s'afficheront:

if(function_exists('error')){
    error();
}